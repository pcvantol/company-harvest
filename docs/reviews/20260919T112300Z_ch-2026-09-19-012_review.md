# Onafhankelijke read-only review CH-2026-09-19-012

Reviewer: onafhankelijke subagent. Datum: 2026-09-19.
Disposition: **PASS**, geen resterende P0/P1/P2.

De reviewer inspecteerde de huidige diff en de volledige regressie-uitvoer
zonder bestanden te wijzigen of KVK-netwerkverkeer te starten. Tijdens de
review gevonden en vóór PASS verholpen:

- P1: stale `run.lock` blokkeerde hervatten na harde crash. Herstel gebeurt
  nu alleen bij bewezen dode eigenaar, geserialiseerd via SQLite; onbekende
  of actieve eigenaar houdt de lock.
- P1: progress kon `COMPLETE` worden vóór artefactregistratie. Er is nu een
  `FINALIZING`-tussenstatus; `COMPLETE` volgt pas na succesvolle publicatie.
- P1: audit kende de gefilterde pre-KVK-kandidaatpartitie niet. Zij gebruikt
  `pre_kvk_eligible` voor de exacte terminale closure wanneer beschikbaar.
- P2: onzekere/mislukte verzoeken waren ten onrechte als hervatbaar gemarkeerd.
  Zij blijven unresolved zonder automatische retry.
- P2: outcome-rapportage gebruikte voor deze route nul generieke kandidaten.
  De KVK-terminalovergang gebruikt nu de pre-KVK-eligible-partitie; bestaande
  raw-/dedupmetrics blijven onveranderd.

Nacontrole: 138/138 tests groen; `git diff --check` schoon; alle 21 eigen
uitvoerbare Pythonbestanden strikt >80% statementcoverage (laagste 80,12%);
`pre_kvk_kvk.py` 268/300 statements. Gecontroleerd zijn onder meer publieke
HTTP-route, één pagina/poging, >=2s pacing, expliciete volledige scope,
locks/crashherstel, IN_FLIGHT vóór GET, UNKNOWN zonder resend,
401/403/429-stop, journalreconstructie, hashgebonden closure, audit,
rapportage en partial-exportguard.

Niet-blokkerende grenzen: een crash precies tijdens aanmaak van een nog lege
of corrupte runlock vereist conservatieve handmatige lockcontrole. Veel
kleine `--max-requests`-sessies herscannen de grote eligible-TSV; voor de
lange doorloop is één sessie of een beperkt aantal hervattingen bedoeld.
De reviewer deed geen live KVK-call en kwalificeerde geen externe
gebruiksvoorwaarden of volledige schaalrun.
