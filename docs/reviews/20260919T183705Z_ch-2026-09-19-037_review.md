# Onafhankelijke read-only review CH-2026-09-19-037

Reviewer: subagent `/root/release_independent_review`. Geen edits, live
harvest, Git-commit of publicatie door de reviewer.

De eerste review signaleerde: onopgeslagen 3.0.0-bronwijzigingen,
ontbrekende releasenotities, verouderde ongepubliceerd-claims, in de
sandbox schijnbaar onbereikbaar GitHub, en twee gaten in de directe
`release.py publish`-route (geen concept-bytecontrole en onvoldoende
onderscheid tussen afwezige release en API-fout). Daarnaast sloeg de
assetscan archiefleden boven 5 MiB over.

Afhandeling vóór broncommit: GitHub-authenticatie, rechten, publieke
repository en bestaande release zijn met geautoriseerde netwerktoegang
bevestigd; v3.0.0-releasenotities en actuele documentatie zijn bijgewerkt;
de scanner faalt nu gesloten op te grote archiefleden met regressietest.
De publicatie gebruikt uitdrukkelijk een handmatige gefaseerde conceptroute
met bytecontrole, **niet** de directe `publish`-helper.

Finale kandidaatdiffreview: **PASS voor lokale broncommit**, geen resterende
P0/P1/P2. De reviewer bevestigde `git diff --check`; de hoofdagent draaide
de volledige lokale quality gate: 255 tests, per eigen Pythonbestand strikt
meer dan 80% statementcoverage, Ruff, mypy en publicatiescan PASS, plus een
aparte gemockte E2E-integratieproef PASS.

Deze review is geen vroegtijdige release-PASS. Exacte-commit-CI op macOS en
Windows, schone build, geïsoleerde wheelinstallatie, conceptassetvergelijking,
publieke herdownload en anonieme herinstallatie zijn afzonderlijke
publicatiepoorten. De uitkomst daarvan staat in het latere releasebewijs.

## Onafhankelijke postpublicatiecontrole

Na publicatie heeft dezelfde read-only reviewer de lokale/remote tag,
publieke GitHub Release-API, assetnamen/-groottes/-digests en exacte-commit-CI
opnieuw gelezen. `v3.0.0` wijst naar `12b861e1c0298afee5396154883c67d8744246aa`;
de release is publiek met vijf juiste assets en beide CI-platformjobs
`success`. De teruggedownloade concept- en publieke mappen waren
byte-identiek aan de build (`diff -rq` zonder verschillen); verificatie van
de publieke manifestset gaf PASS. Een kale HTTPS-download zonder
GitHub-token leverde dezelfde wheel-SHA. De nieuwe bewijs-/resultaatdocs
en `git diff --check` hebben **PASS, geen feitelijke blockers**. De eerdere
historische conceptstatus is gebaseerd op de direct na upload gelezen API;
de reviewer kon achteraf de conceptbytes, maar niet die vroegere status,
zelfstandig controleren.
