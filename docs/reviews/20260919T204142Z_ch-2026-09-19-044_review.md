# Onafhankelijke read-only review CH-2026-09-19-044

Reviewer: subagent `/root/v4_build_review`; de reviewer wijzigde geen
bestanden en startte geen live harvest of externe publicatie. Eindoordeel:
**PASS**, zonder P0/P1/P2-blokkades voor een **lokale** 4.0.0-bouw.

De reviewer controleerde onafhankelijk het manifest, alle vier geregistreerde
SHA-256-hashes en `tools/release.py verify` (PASS). Wheel-METADATA noemt
`company-lookup` 4.0.0 met `>=3.14,<3.15`; entrypoint is
`company-lookup = company_lookup.cli:main`. Alle 22 pakketbestanden in
wheel en sdist komen byte-identiek overeen met broncommit `f576c4d`.
De sdist bevat alle 332 getrackte bronbestanden byte-identiek, plus het
gegenereerde `PKG-INFO`; geen getrackt bestand ontbreekt. De wheel in de
bundel is byte-identiek met de losse wheel; alle 11 gebundelde README-,
script- en requirementsbestanden komen overeen met dezelfde commit.
De archieven bevatten geen verboden run-, `.local`-, `.git`-, `.env`-,
log-, JSONL- of SQLite-bestandsnamen; de assetscanner slaagt. De oude
productnaam ontbreekt in actieve code, scripts, tools en CI.

Bewijsgrens: de schone-installatie- en workflowtest is door de builder
uitgevoerd en als `PASS` in het manifest vastgelegd, maar de tijdelijke
venv en installatielog zijn na de build verwijderd; de reviewer kon deze
niet zelfstandig achteraf inspecteren. Er is geen live bron-/KVK-harvest,
remote release of anonieme download beoordeeld. Voor publicatie is een
nieuwe build van de dan actuele evidencecommit vereist; een v4-
releasenotitie ontbreekt nog en valt buiten deze lokale bouwopdracht.

Het gecontroleerde [bouwbewijs](../releases/20260919-v4.0.0-local-build.md)
en het [resultaat](../prompts/20260919T204142Z_ch-2026-09-19-044_result.md)
maken de lokale status en beperkingen expliciet. Geen herstelpunten open
binnen de opgedragen scope.
