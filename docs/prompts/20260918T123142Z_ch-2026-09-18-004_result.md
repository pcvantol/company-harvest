# Resultaat CH-2026-09-18-004

- Status: `COMPLETED`
- Execution ID: `CH-2026-09-18-004`
- Versie-impact: nieuwe functionaliteit voor een volgende minor release; nog geen publicatie
- Release: geen

R1 is als verticale CLI-slice voltooid:

- broncatalogusschema 2 bevat identifier-, toegang-, voorwaarden-, actualiteits-, meet-, bronfamilie-, inclusie-, provenance- en laagprofielen;
- legacycatalogi worden verliesvrij en immutable gemigreerd;
- lokale CSV/TSV/XLSX/HTML-bronnen mogen zonder KVK-kolom worden geïmporteerd; `MISSING` en `INVALID` blijven kandidaten;
- conservatieve deduplicatie voegt naamgenoten zonder geldig nummer niet samen en routeert meerdere geldige hints bij dezelfde naam naar conflict;
- `outcome_report.json` en het Markdownrapport meten per bron, kandidaten, overlap/concentratie, resources/groei en closure over zes procesovergangen;
- deduplicatie en rapportage gebruiken beide uitsluitend de nieuwste complete versie per bron;
- directe HTTP-clients, de Playwright-browsercontext, runmetadata en rapportage gebruiken aantoonbaar `company-lookup/0.1`;
- 36 tests, lint, typecheck, publicatiescan en de per-file coveragegate zijn lokaal geslaagd;
- onafhankelijke review eindigde met `PASS` zonder resterende P0-, P1- of P2-bevindingen.

De eerste repository-CI-run vond na de functionele oplevering uitsluitend een
Windows-typecheckprobleem rond de POSIX-module `resource`. De portabilitycorrectie
gebruikt op Windows expliciet `UNAVAILABLE_ON_PLATFORM` en laadt de module elders
dynamisch; de byte-eenheden op macOS en Linux blijven gelijk. De regressietest, native
typecheck en `mypy --platform win32` zijn geslaagd. De onafhankelijke herreview gaf
opnieuw `PASS` zonder P0-, P1- of P2-bevindingen.

Er is geen live bronmeting, productieharvest of release uitgevoerd. R2 is nu het eerstvolgende increment.
