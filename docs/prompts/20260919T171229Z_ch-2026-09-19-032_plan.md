# Plan CH-2026-09-19-032

1. Verwijder `--export-limit` uit `run e2e` en `--limit` uit de losse
   `export`-opdracht; laat de exportservice alle actieve rijen leveren.
2. Houd de bestaande outputset/audit compatibel: het reservebestand blijft
   als lege schema-1-compatibiliteitsledger, niet als afgekapte populatie.
3. Bewaak hervatten van oude E2E-runs: al geëxporteerde runs mogen worden
   gecontroleerd; alleen een oude limiet die aantoonbaar nooit bindend kon
   zijn mag zonder semantiekverandering worden hervat. Andere oude runs
   stoppen met een duidelijke migratiefout.
4. Voeg regressies toe voor meerdere actieve bedrijven, begrensde KVK-cohort,
   CLI-afwijzing van verwijderde opties, hervatten en audit/closure.
5. Werk actuele Nederlandstalige handleiding, ADR/traceability/changelog en
   promptresultaat bij; voer per-file coverage, lint, types, publicatiescan,
   offline E2E en een onafhankelijke read-only subagentreview uit.
